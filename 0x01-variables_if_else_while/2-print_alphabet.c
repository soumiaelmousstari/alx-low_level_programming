#include <stdio.h>
/**
 * main -program that prints the alphabet in lowercase.
 * Return: 0 succecc instructions
*/
int main(void)
{
	int a = 97;

	while (a <= 122)
	{
		putchar(a);
		a++;
	}
return (0);
}
