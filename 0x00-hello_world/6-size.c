#include <stdio.h>
/**
 * main -program that prints the size of various types on the compute * r it is compiled and run on.
 * Return: 0 successful execution
*/
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float f
puts("Size of a char:%lu byte(s)\n",(unsigned long)sizeof(a));
puts("Size of a char:%lu byte(s)\n",(unsigned long)sizeof(b));
puts("Size of a char:%lu byte(s)\n",(unsigned long)sizeof(c));
puts("Size of a char:%lu byte(s)\n",(unsigned long)sizeof(d));
puts("Size of a char:%lu byte(s)\n",(unsigned long)sizeof(f));
return (0);
}
